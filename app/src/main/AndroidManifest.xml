<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.li.calorie_run">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- 增加文件存储和访问摄像头的权限 -->
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/s_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/s_logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:hardwareAccelerated="false"
        android:largeHeap="true"
        >
        <activity
            android:name=".view.MainActivity"
            android:label="@string/app_name" />
        <activity android:name=".view.LoginActivity" />
        <activity android:name=".view.RegisterConfirmActivity" />
        <activity android:name=".view.RegisterActivity" />
        <activity android:name=".view.WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.SportsPartnerActivity" />
        <activity android:name=".view.SportsIssueActivity" />
        <activity android:name=".view.SportDetailActivity" />
        <activity android:name=".view.MeActivity" />
        <activity android:name=".view.mainnavigations.index_camera.activity.CameraActivity" />
    </application>

</manifest>